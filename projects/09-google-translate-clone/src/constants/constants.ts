export enum Language {
  EN = 'English',
  ES = 'Spanish',
  DE = 'Deutsch',
  FR = 'French'
}

export const AutoLanguage = 'Auto'

export enum TranslatorTypes {
  SWAP_LANGUAGES,
  SET_FROM_LANGUAGE,
  SET_TO_LANGUAGE,
  SET_FROM_TEXT,
  SET_RESULT
}

export enum FromTo {
  From = 'from',
  To = 'to'
}
